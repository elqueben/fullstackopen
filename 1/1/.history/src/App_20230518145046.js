const App = () => {
  // const-definitions

  let Header = (props) => {
    let course = 
  }

  return (
    <div>
      <Header course={course} />
      <Content />
      <Total />
    </div>
  )
}

export default App