const App = () => {
  // const-definitions

  let Header = (props) => {
    course = 
  }

  return (
    <div>
      <Header course={course} />
      <Content />
      <Total />
    </div>
  )
}

export default App